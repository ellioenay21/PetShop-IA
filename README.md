# PetShop IA

Projeto ASP.NET Core MVC para gerenciar clientes e pets. Contém camadas de Repository e Service, validação de CPF e persistência com SQLite.

Como executar localmente:

```powershell
dotnet build
dotnet run --project Petshop/Petshop.csproj
```
